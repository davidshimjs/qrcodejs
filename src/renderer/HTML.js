(function (global) {
    // namespace
    global.qrcode = global.qrcode || {};
    var qrcode = global.qrcode;

    var HTMLRenderer = function () {
    };

    HTMLRenderer.prototype = {
    };

    qrcode.HTMLRenderer = Renderer;
})(this);